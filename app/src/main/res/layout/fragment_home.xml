<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gmy_primary"
    android:fillViewport="true"
    tools:context=".ui.dashboard.HomeFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Encabezado -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp">

            <TextView
                android:id="@+id/tv_header_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Resumen General"
                android:textColor="@color/white"
                android:textSize="28sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/tv_header_title"
                android:text="Panel de Control"
                android:textColor="#B0BEC5"
                android:textSize="16sp" />

            <ImageView
                android:id="@+id/settings_icon"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                android:src="@drawable/ic_settings_24"
                app:tint="@color/gmy_accent" />
        </RelativeLayout>

        <!-- Tarjetas de Estadísticas -->
        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:rowCount="2"
            android:alignmentMode="alignMargins"
            android:columnOrderPreserved="false">

            <!-- Activos -->
            <androidx.cardview.widget.CardView style="@style/DashboardCard">
                <RelativeLayout style="@style/DashboardCardContent">
                    <ImageView style="@style/DashboardCardIcon" android:src="@drawable/ic_check_circle_24" app:tint="@color/status_active"/>
                    <LinearLayout style="@style/DashboardCardTextLayout">
                        <TextView android:id="@+id/tvActiveCount" style="@style/DashboardCardNumber" android:textColor="@color/status_active" tools:text="13"/>
                        <TextView style="@style/DashboardCardLabel" android:text="Activos"/>
                    </LinearLayout>
                </RelativeLayout>
            </androidx.cardview.widget.CardView>

            <!-- Por Vencer -->
            <androidx.cardview.widget.CardView style="@style/DashboardCard">
                <RelativeLayout style="@style/DashboardCardContent">
                    <ImageView style="@style/DashboardCardIcon" android:src="@drawable/ic_hourglass_24" app:tint="@color/status_warning"/>
                    <LinearLayout style="@style/DashboardCardTextLayout">
                        <TextView android:id="@+id/tvExpiringCount" style="@style/DashboardCardNumber" android:textColor="@color/status_warning" tools:text="2"/>
                        <TextView style="@style/DashboardCardLabel" android:text="Por Vencer"/>
                    </LinearLayout>
                </RelativeLayout>
            </androidx.cardview.widget.CardView>

            <!-- Vencidos -->
            <androidx.cardview.widget.CardView style="@style/DashboardCard">
                <RelativeLayout style="@style/DashboardCardContent">
                    <ImageView style="@style/DashboardCardIcon" android:src="@drawable/ic_warning_24" app:tint="@color/status_expired"/>
                    <LinearLayout style="@style/DashboardCardTextLayout">
                        <TextView android:id="@+id/tvExpiredCount" style="@style/DashboardCardNumber" android:textColor="@color/status_expired" tools:text="0"/>
                        <TextView style="@style/DashboardCardLabel" android:text="Vencidos"/>
                    </LinearLayout>
                </RelativeLayout>
            </androidx.cardview.widget.CardView>

            <!-- Total Socios -->
            <androidx.cardview.widget.CardView style="@style/DashboardCard">
                <RelativeLayout style="@style/DashboardCardContent">
                    <ImageView style="@style/DashboardCardIcon" android:src="@drawable/ic_groups_24" app:tint="#B0BEC5"/>
                    <LinearLayout style="@style/DashboardCardTextLayout">
                        <TextView android:id="@+id/tvTotalCount" style="@style/DashboardCardNumber" tools:text="15"/>
                        <TextView style="@style/DashboardCardLabel" android:text="Total Socios"/>
                    </LinearLayout>
                </RelativeLayout>
            </androidx.cardview.widget.CardView>

        </GridLayout>

        <!-- Sección del Mapa -->
        <TextView
            android:text="Nuestras Sucursales en Ameca"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginTop="30dp"
            android:layout_marginBottom="8dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="Toca el mapa para explorar"
            android:textColor="#B0BEC5"
            android:textSize="14sp" />

        <androidx.cardview.widget.CardView
            android:id="@+id/map_card"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            app:cardBackgroundColor="#2C2C2C"
            app:cardCornerRadius="16dp"
            app:cardElevation="6dp">

            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/map_container"
                android:name="com.google.android.gms.maps.SupportMapFragment"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

        </androidx.cardview.widget.CardView>

    </LinearLayout>
</androidx.core.widget.NestedScrollView>